services:
  - type: web
    name: dontraval-app
    env: python
    region: oregon
    plan: free

    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"

    branch: main
    repo: https://github.com/Nouri11190/dontraval-frontent

    domains:
      - dontraval.it.com
